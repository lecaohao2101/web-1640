<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Faculty</title>

    <style>
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
        }

        .topnav {
            overflow: hidden;
            background-color: #3a7efc;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            font-weight: bold;
        }

        .topnav a {
            color: #f2f2f2;
            text-align: center;
            padding: 14px 13px;
            text-decoration: none;
            font-size: 17px;
            font-weight: bold;
        }

        .topnav a:hover {
            background-color: #3499d0;
            color: black;
        }

        .topnav a.active {
            color: white;
        }
        .logo img {
            height: 50px;
            width: auto;
            vertical-align: middle;
            margin-right: 10px;
        }
        .nav{
            flex: 1;
        }

        button {
            background-color: #3a7efc;
            color: white;
            padding: 14px 30px;
            margin: 8px 0;
            border: none;
            cursor: pointer;
            width: 100%;
            border-radius: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            font-size: 17px;
            font-weight: bold;
        }

        button:hover {
            opacity: 0.8;
            background-color: #3499d0;

        }

        .container.mt-5 {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .btn-primary,
        .btn-danger {
            padding: 10px 20px;
            margin-right: 10px;
        }

        .btn-container {
            display: flex;
            align-items: center;
        }

        @media screen and (max-width: 768px) {
            .topnav {
                flex-direction: column;
                align-items: stretch;
            }

            .nav {
                margin-top: 10px;
            }

            .btn-container {
                flex-direction: column;
                align-items: flex-start;
            }

            .btn-primary,
            .btn-danger {
                margin-right: 0;
                margin-bottom: 5px;
            }

            .container.mt-5 {
                padding: 10px;
            }
        }

    </style>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>


<div class="topnav">
    <div class="logo"><img  src="https://cdn.haitrieu.com/wp-content/uploads/2022/12/Icon-Truong-Dai-hoc-Greenwich-Viet-Nam.png" alt="Logo"></div>

    <div class="nav">
        <a href="coordinator-page">Home</a>
        <a href="coordinator-manage-student">Manage Student</a>
        <a href="coordinator-manage-post">Manage Post</a>
    </div>

    <div>
        <form action="/login_page" method="get">
            <button>Logout</button>
        </form>
    </div>

</div>

<div class="container mt-5">

    <a href="/coordinator/coordinator-add-faculty" class="btn btn-primary">Add Faculty</a>

    <h1>Manage Faculty</h1>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Faculty Name</th>
            <th scope="col">Actions</th>
        </tr>
        </thead>
        <tbody>
        <% departments.forEach((department) => { %>
            <tr>
                <td><%= department.department_id %></td>
                <td><%= department.department_name %></td>
                <td class="btn-container">
                    <a href="/coordinator/coordinator-edit-faculty/<%= department.department_id %>" class="btn btn-primary">Edit</a>
                    <form action="/coordinator/coordinator-delete-faculty/<%= department.department_id %>" method="post">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>

            </tr>
        <% }); %>
        </tbody>
    </table>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.10.2/umd/popper.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/js/bootstrap.min.js"></script>
</body>
</html>
